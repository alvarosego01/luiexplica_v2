{% block html_head_container %}

	{# {% include 'partial/html-header.twig' %} #}
	{% include 'partial/head.twig' %}
	{% block head %}{% endblock %}

{% endblock %}



<body
	class="{{body_class}} font-inter antialiased bg-white text-slate-800 tracking-tight"
    data-template="base.twig"
    >

	<!-- Page wrapper -->
	<div
		class="flex flex-col min-h-screen overflow-hidden">

		{# Header #}
		{% include 'partial/header.twig' %}

		<main class="grow">

            {{ post.content }}

        </main>


	</div>

	{# <body class="{{body_class}}" data-template="base.twig">

			{{ function('wp_body_open') }}
			<a class="skip-link screen-reader-text" href="#content">{{ _e( 'Skip to content') }}</a>

	        {% include "partial/header.twig" with {
	            'items': menu.get_items,
	            'site': site
	        } %}

			<section id="content" role="main" class="content-wrapper">
				{% if title %}<h1>{{title}}</h1>{% endif %}
				<div class="wrapper">
					{% block content %}
						Sorry, no content
					{% endblock %}
				</div>
				{% if sidebar %}
					<aside class="layout-sidebar">
						{{sidebar}}
					</aside>
				{% endif %}
			</section>

	         {% include "partial/footer.twig" %}

		</body> #}


	{% include "partial/footer.twig" %}

</body></html>



